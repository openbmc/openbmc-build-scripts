{
    "extends": ["plugin:json/recommended-with-comments"],

    // Generated json files created by meson does not pass
    // the standard eslint json validation, so ignoring them.

    "ignorePatterns":["**/meson-*/*.json"]
}
